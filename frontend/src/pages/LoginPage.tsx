export function LoginPage() {
  return (
    <section className="mx-auto max-w-xl rounded-2xl border border-[var(--c-border)] bg-[var(--c-panel)] p-6 shadow-sm">
      <h2 className="text-2xl font-semibold">Login / Signup</h2>
      <p className="mt-2 text-sm text-[var(--c-muted)]">
        Authentication screens are scaffolded in Phase 1. Full auth flows begin in Phase 2.
      </p>
    </section>
  );
}
